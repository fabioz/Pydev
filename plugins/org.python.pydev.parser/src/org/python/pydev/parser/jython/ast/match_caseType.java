// Autogenerated AST node
package org.python.pydev.parser.jython.ast;

import org.python.pydev.parser.jython.SimpleNode;
import java.util.Arrays;

public final class match_caseType extends SimpleNode {
    public patternType pattern;
    public exprType guard;
    public stmtType[] body;

    public match_caseType(patternType pattern, exprType guard, stmtType[] body) {
        this.pattern = pattern;
        this.guard = guard;
        this.body = body;
    }

    @Override
    public int hashCode() {
        final int prime = 31;
        int result = 1;
        result = prime * result + ((pattern == null) ? 0 : pattern.hashCode());
        result = prime * result + ((guard == null) ? 0 : guard.hashCode());
        result = prime * result + Arrays.hashCode(body);
        return result;
    }

    @Override
    public boolean equals(Object obj) {
        if (this == obj) return true;
        if (obj == null) return false;
        if (getClass() != obj.getClass()) return false;
        match_caseType other = (match_caseType) obj;
        if (pattern == null) { if (other.pattern != null) return false;}
        else if (!pattern.equals(other.pattern)) return false;
        if (guard == null) { if (other.guard != null) return false;}
        else if (!guard.equals(other.guard)) return false;
        if (!Arrays.equals(body, other.body)) return false;
        return true;
    }
    @Override
    public match_caseType createCopy() {
        return createCopy(true);
    }
    @Override
    public match_caseType createCopy(boolean copyComments) {
        stmtType[] new0;
        if(this.body != null){
        new0 = new stmtType[this.body.length];
        for(int i=0;i<this.body.length;i++){
            new0[i] = (stmtType) (this.body[i] != null? this.body[i].createCopy(copyComments):null);
        }
        }else{
            new0 = this.body;
        }
        match_caseType temp = new
        match_caseType(pattern!=null?(patternType)pattern.createCopy(copyComments):null,
        guard!=null?(exprType)guard.createCopy(copyComments):null, new0);
        temp.beginLine = this.beginLine;
        temp.beginColumn = this.beginColumn;
        if(this.specialsBefore != null && copyComments){
            for(Object o:this.specialsBefore){
                if(o instanceof commentType){
                    commentType commentType = (commentType) o;
                    temp.getSpecialsBefore().add(commentType.createCopy(copyComments));
                }
            }
        }
        if(this.specialsAfter != null && copyComments){
            for(Object o:this.specialsAfter){
                if(o instanceof commentType){
                    commentType commentType = (commentType) o;
                    temp.getSpecialsAfter().add(commentType.createCopy(copyComments));
                }
            }
        }
        return temp;
    }

    @Override
    public String toString() {
        StringBuffer sb = new StringBuffer("match_case[");
        sb.append("pattern=");
        sb.append(dumpThis(this.pattern));
        sb.append(", ");
        sb.append("guard=");
        sb.append(dumpThis(this.guard));
        sb.append(", ");
        sb.append("body=");
        sb.append(dumpThis(this.body));
        sb.append("]");
        return sb.toString();
    }

    @Override
    public Object accept(VisitorIF visitor) throws Exception {
        if (visitor instanceof VisitorBase) {
            ((VisitorBase) visitor).traverse(this);
        } else {
            traverse(visitor);
        }
        return null;
    }

    @Override
    public void traverse(VisitorIF visitor) throws Exception {
        if (pattern != null) {
            pattern.accept(visitor);
        }
        if (guard != null) {
            guard.accept(visitor);
        }
        if (body != null) {
            for (int i = 0; i < body.length; i++) {
                if (body[i] != null) {
                    body[i].accept(visitor);
                }
            }
        }
    }

}
